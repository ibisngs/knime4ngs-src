<?xml version="1.0" encoding="UTF-8"?>
<knimeNode icon="./default.png" type="Source" xmlns="http://knime.org/node/v2.8" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://knime.org/node/v2.10 http://knime.org/node/v2.10.xsd">
    <name>CombineVCFs</name>
    
    <shortDescription>
       Combines any number of VCF lists that were produced by Pindel into a single joint VCF file. 
    </shortDescription>
    
    <fullDescription>
        <intro>
        	Analogous to the CombineGVCFs the CombineVCFs can be used for hierarchical merging of individual VCF lists of different samples.
        	The resulting VCF file can then be further processed by filtering nodes.<br />
        	For further information, see: <a href="https://www.broadinstitute.org/gatk/guide/tooldocs/org_broadinstitute_gatk_tools_walkers_variantutils_CombineGVCFs.php">GATK documentation of CombineGVCFs</a>
       	 </intro>
        
        <tab name="Options">
        	<option name="HTE">
        		Set a threshold for repeated execution.
        		Only used if HTE is enabled in the preference page.
        	</option>
        	<option name="Path to GATK jar file">
        		Set the path to GenomeAnalysisTK.jar and memory usage.
        	</option>
        	<option name="Reference Genome">
        		Set the path to a reference genome.
        	</option>
        	<option name="Path to BED file">
        		Coverage analysis can be restricted to genomic regions defined in a BED file.
        	</option>
        	<option name="GenotypeMergeType">
        		<li>
        		Uniquify:
    			Make all sample genotypes unique by file.
    			Each sample shared across RODs gets named sample.ROD.
    			</li>
    			<li>
				Unsorted:
    			Take the genotypes in any order.
    			</li>
				<li>
				Require unique:
    			Require that all samples/genotypes be unique between all inputs.
    			</li>
			</option>
			<option name="Further options">
        		Commit further options to GATK.
        	</option>
			<option name="Path to output files">
        		Set the path to the folder where the combined gvcf files should be stored.
        	</option> 
        </tab>
    </fullDescription>
    
    <ports>
        <inPort index="0" name="VCF Inport">VCF files in the first column</inPort>
        <outPort index="0" name="Out-Port">Merged VCF in the first column</outPort>
    </ports>   
</knimeNode>
